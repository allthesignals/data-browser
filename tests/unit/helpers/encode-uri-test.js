
import { encodeUri } from 'data-browser/helpers/encode-uri';
import { module, test } from 'qunit';

module('Unit | Helper | encode uri');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = encodeUri([42]);
  assert.ok(result);
});

