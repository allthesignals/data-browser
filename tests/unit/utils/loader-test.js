import loader from 'data-browser/utils/loader';
import { module, test } from 'qunit';

module('Unit | Utility | loader');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = loader();
  assert.ok(result);
});
