import evaluateSpatiality from 'data-browser/utils/evaluate-spatiality';
import { module, test } from 'qunit';

module('Unit | Utility | evaluate spatiality');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = evaluateSpatiality();
  assert.ok(result);
});
